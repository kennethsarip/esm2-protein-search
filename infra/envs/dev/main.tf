# Empty on purpose. D1 only needs this root module to init against remote
# state and plan cleanly, per CLAUDE.md D1's done-when criteria. D2 adds the
# storage, network, and registry module calls here.
