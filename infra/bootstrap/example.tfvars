github_org  = "kennethsarip"
github_repo = "esm2-protein-search"

# Defaults in variables.tf are fine for everything else. Override here only
# if the default state bucket name is already taken; S3 bucket names are
# globally unique across all of AWS, not just this account.
